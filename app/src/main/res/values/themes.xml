<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SalaryPrediction" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SalaryPrediction" parent="Base.Theme.SalaryPrediction" />

    <!-- Splash Screen Theme -->
    <style name="Theme.SplashScreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/splash_navy_blue</item>
        <item name="android:statusBarColor">@color/splash_navy_blue</item>
        <item name="android:navigationBarColor">@color/splash_navy_blue</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>
